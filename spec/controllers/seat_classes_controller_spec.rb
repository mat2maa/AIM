require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe SeatClassesController do

  # This should return the minimal set of attributes required to create a valid
  # SeatClass. As you add validations to SeatClass, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) { { "name" => "MyString" } }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # SeatClassesController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET index" do
    it "assigns all seat_classes as @seat_classes" do
      seat_class = SeatClass.create! valid_attributes
      get :index, {}, valid_session
      assigns(:seat_classes).should eq([seat_class])
    end
  end

  describe "GET show" do
    it "assigns the requested seat_class as @seat_class" do
      seat_class = SeatClass.create! valid_attributes
      get :show, {:id => seat_class.to_param}, valid_session
      assigns(:seat_class).should eq(seat_class)
    end
  end

  describe "GET new" do
    it "assigns a new seat_class as @seat_class" do
      get :new, {}, valid_session
      assigns(:seat_class).should be_a_new(SeatClass)
    end
  end

  describe "GET edit" do
    it "assigns the requested seat_class as @seat_class" do
      seat_class = SeatClass.create! valid_attributes
      get :edit, {:id => seat_class.to_param}, valid_session
      assigns(:seat_class).should eq(seat_class)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new SeatClass" do
        expect {
          post :create, {:seat_class => valid_attributes}, valid_session
        }.to change(SeatClass, :count).by(1)
      end

      it "assigns a newly created seat_class as @seat_class" do
        post :create, {:seat_class => valid_attributes}, valid_session
        assigns(:seat_class).should be_a(SeatClass)
        assigns(:seat_class).should be_persisted
      end

      it "redirects to the created seat_class" do
        post :create, {:seat_class => valid_attributes}, valid_session
        response.should redirect_to(SeatClass.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved seat_class as @seat_class" do
        # Trigger the behavior that occurs when invalid params are submitted
        SeatClass.any_instance.stub(:save).and_return(false)
        post :create, {:seat_class => { "name" => "invalid value" }}, valid_session
        assigns(:seat_class).should be_a_new(SeatClass)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        SeatClass.any_instance.stub(:save).and_return(false)
        post :create, {:seat_class => { "name" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested seat_class" do
        seat_class = SeatClass.create! valid_attributes
        # Assuming there are no other seat_classes in the database, this
        # specifies that the SeatClass created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        SeatClass.any_instance.should_receive(:update_attributes).with({ "name" => "MyString" })
        put :update, {:id => seat_class.to_param, :seat_class => { "name" => "MyString" }}, valid_session
      end

      it "assigns the requested seat_class as @seat_class" do
        seat_class = SeatClass.create! valid_attributes
        put :update, {:id => seat_class.to_param, :seat_class => valid_attributes}, valid_session
        assigns(:seat_class).should eq(seat_class)
      end

      it "redirects to the seat_class" do
        seat_class = SeatClass.create! valid_attributes
        put :update, {:id => seat_class.to_param, :seat_class => valid_attributes}, valid_session
        response.should redirect_to(seat_class)
      end
    end

    describe "with invalid params" do
      it "assigns the seat_class as @seat_class" do
        seat_class = SeatClass.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SeatClass.any_instance.stub(:save).and_return(false)
        put :update, {:id => seat_class.to_param, :seat_class => { "name" => "invalid value" }}, valid_session
        assigns(:seat_class).should eq(seat_class)
      end

      it "re-renders the 'edit' template" do
        seat_class = SeatClass.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SeatClass.any_instance.stub(:save).and_return(false)
        put :update, {:id => seat_class.to_param, :seat_class => { "name" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested seat_class" do
      seat_class = SeatClass.create! valid_attributes
      expect {
        delete :destroy, {:id => seat_class.to_param}, valid_session
      }.to change(SeatClass, :count).by(-1)
    end

    it "redirects to the seat_classes list" do
      seat_class = SeatClass.create! valid_attributes
      delete :destroy, {:id => seat_class.to_param}, valid_session
      response.should redirect_to(seat_classes_url)
    end
  end

end
